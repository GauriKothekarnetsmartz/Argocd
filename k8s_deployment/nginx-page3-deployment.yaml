apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-page3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-page3
  template:
    metadata:
      labels:
        app: nginx-page3
    spec:
      containers:
      - name: nginx-page3
        image: nginx:latest
        volumeMounts:
        - name: html-volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-volume
        configMap:
          name: nginx-page3-html
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-page3-service
spec:
  selector:
    app: nginx-page3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

